on:
  pull_request:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test

      - name: Deploy
        run: |
          curl "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"